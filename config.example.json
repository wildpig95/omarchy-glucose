{
  "_comment": "复制到 ~/.config/omarchy/glucose/config.json，然后 chmod 600。这个文件里有密码，别提交到任何仓库。",

  "source": "mock",

  "historyHours": 24,

  "librelinkup": {
    "_comment": "瞬感通 / LibreLinkUp 账号。密码不要写在这里：运行 scripts/glucose-fetch.py --set-credentials 输入一次，存进系统 keyring。这里只保留邮箱（非机密）和区域。也可用环境变量 GLUCOSE_LLU_EMAIL / GLUCOSE_LLU_PASSWORD。",
    "email": "",
    "region": "cn",
    "patientId": ""
  },

  "nightscout": {
    "_comment": "自建 Nightscout 时填这里，并把上面的 source 改成 nightscout。tokenType: secret（填管理后台的 API secret）或 token（填只读 token）。",
    "url": "",
    "token": "",
    "tokenType": "secret",
    "targetLow": 70,
    "targetHigh": 180,
    "patientName": ""
  },

  "mock": {
    "_comment": "用于开发和预览的假数据。",
    "seed": 7
  },

  "history": {
    "_comment": "滚动历史：LibreLinkUp 的 graph 接口只返回约 12 小时，所以要靠插件把每次轮询的点累积起来，昨天/前天才有数据可看。默认存 ~/.local/state/glucose/history.json。",
    "enabled": true,
    "retentionHours": 168
  },

  "jev": {
    "_comment": "可选：让 Jev（TypeSafe System One）对当前曲线做一次类型化判断，显示在弹窗里。只做解读，不隐藏读数、不影响报警、不是医疗器械。key 优先取环境变量 TYPESAFE_API_KEY，其次 ~/.winnow/env。",
    "enabled": false,
    "apiKey": "",
    "model": "jev-1.13.0",
    "onlyWhenOutOfRange": true,
    "minIntervalSec": 900
  }
}
